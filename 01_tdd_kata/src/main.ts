import './style.css'

/**
const stageOneFizzBuzz = (): string[] => {
    const fizzBuzzArray = []

    for (let i = 1; i <= 100; i++) {
        fizzBuzzArray.push(StageOneFizzbuzz(i))
    }

    return fizzBuzzArray;
}

const stageTwoFizzBuzz = (): string[] => {
    const fizzBuzzArray = []

    for (let i = 1; i <= 100; i++) {
        fizzBuzzArray.push(StageTwoFizzBuzz(i))
    }

    return fizzBuzzArray;
}

document.querySelector<HTMLDivElement>('#app')!.innerHTML = `
  <div id="kata">
    <div class="number">
        ${Array.from(Array(100).keys()).map((number: number) => number+1).join('<br/>')}    
    </div>
    <div class="stage-one">
        ${stageOneFizzBuzz().join('<br/>')}
    </div>
     <div class="stage-two">
        ${stageTwoFizzBuzz().join('<br/>')}    
    </div>
  </div>
`
 **/